$("#<%= @post.id %> ul").html(
    "<%= escape_javascript(render( partial: 'shared/comment', collection: @post.comments)) %>\
    <li>\
    <%= escape_javascript(render partial: 'shared/comment_form', locals: {post: @post}) %>\
    </li>");